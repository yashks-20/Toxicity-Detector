# Toxicity-Detector
This project is a simple NLP-based toxicity detection model built using TensorFlow and LSTM networks. It takes user text as input and predicts whether the content is toxic across multiple categories.
